<h2>Markers</h2>

<a href="#" class="cancel oh-google-map-close">&times; close</a>

{{#not markers.length}}
<p>There are no markers on the map.</p>
{{/not}}

<ol class="oh-google-map-ordered-list">
{{#each markers}}
	<li>{{#if this.deleted}}<span class="oh-google-map-strike-out">{{/if}}<a href="#" class="marker-center">{{this.address}}</a>{{#if this.deleted}}</span> <a href="#" class="marker-undo oh-google-map-small-text">Undo Delete</a>{{/if}}</li>
{{/each}}
</ol>

<h2>Routes</h2>

<a href="#" class="cancel oh-google-map-close">&times; close</a>

{{#not routes.length}}
<p>There are no routes on the map.</p>
{{/not}}

<ol class="oh-google-map-ordered-list">
{{#forEach routes}}
	<li>{{#if this.deleted}}<span class="oh-google-map-strike-out">{{/if}}<a href="#" class="route-center">{{#if this.title}}{{../this.title}}{{/if}}{{#not this.title}}Route {{../count}}{{/not}}</a>{{#if this.deleted}}</span> <a href="#" class="route-undo oh-google-map-small-text">Undo Delete</a>{{/if}}</li>
{{/forEach}}
</ol>

<h2>Polygons</h2>

{{#not polygons.length}}
<p>There are no polygons on the map.</p>
{{/not}}

<ol class="oh-google-map-ordered-list">
{{#forEach polygons}}
	<li>{{#if this.deleted}}<span class="oh-google-map-strike-out">{{/if}}<a href="#" class="polygon-center">{{#if this.title}}{{../this.title}}{{/if}}{{#not this.title}}Polygon {{../count}}{{/not}}</a>{{#if this.deleted}}</span> <a href="#" class="polygon-undo oh-google-map-small-text">Undo Delete</a>{{/if}}</li>
{{/forEach}}
</ol>

<h2>Polylines</h2>

{{#not polylines.length}}
<p>There are no polylines on the map.</p>
{{/not}}

<ol class="oh-google-map-ordered-list">
{{#forEach polylines}}
	<li>{{#if this.deleted}}<span class="oh-google-map-strike-out">{{/if}}<a href="#" class="polyline-center">{{#if this.title}}{{../this.title}}{{/if}}{{#not this.title}}Polyline {{../count}}{{/not}}</a>{{#if this.deleted}}</span> <a href="#" class="polyline-undo oh-google-map-small-text">Undo Delete</a>{{/if}}</li>
{{/forEach}}
</ol>